# CLAUDE.md

## Project: {{PROJECT_NAME}}

### Tech Stack
- Language: {{LANGUAGE}}
- Entry point: {{ENTRY_POINT}}

### Commands
- Run tests: `{{TEST_COMMAND}}`
- Start dev: (fill in)
- Pack context: `bash scripts/pack_context.sh`

### Architecture
(Describe your project structure here — or run `bash scripts/pack_context.sh`
and reference `docs/context_map.txt`, which is local-only and auto-regenerated
by the pre-commit hook)

### Conventions
- Follow CONTRIBUTING.md for all commit and code standards
- Consider updating `docs/memory/active_context.md` when context would be useful for the next session
- Log architectural decisions in `docs/memory/decisions.md` when significant choices are made
- (Add project-specific conventions here)
